gymnasium>=0.29,<1.1
stable-baselines3>=2.2,<3.0
shimmy>=1.3,<3.0
pybullet>=3.2.5,<4.0
numpy>=1.23,<2.0

# train.py passes tensorboard_log=..., and SB3 raises ImportError at the start
# of learn() if tensorboard is missing.
tensorboard>=2.11,<3.0

# Vision pipeline (my_vision.py). face-recognition builds dlib from source,
# which needs build-essential + cmake (see Dockerfile).
opencv-python>=4.8,<5.0
face-recognition>=1.3,<2.0
